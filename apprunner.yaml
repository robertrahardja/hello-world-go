version: 1.0
runtime: go1
build:
  commands:
    build:
      - go install github.com/a-h/templ/cmd/templ@latest
      - go get github.com/a-h/templ
      - templ generate
      - go mod tidy
      - go build -o server
run:
  command: ./server
  network:
    port: 8080
